project:
  profile:
    name: metis
  local:
    hostname: metis.docksal
drush:
  aliases:
    local: docksal.default
drupal:
  db:
    database: default
    username: user
    password: user
    host: db
default-content:
  path: ${repo.root}/docroot/profiles/custom/metis/modules/metis_default_content
